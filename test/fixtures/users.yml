# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# Senha de ambos: "password123" (use TEST_PASSWORD nos testes de login)

fernanda:
  email: fernanda@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>

visitante:
  email: visitante@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>

# Sem game_rounds — usado para testar os casos de "zero jogos" (guards de
# divisão por zero nas porcentagens e o estado vazio da página de estatísticas)
novato:
  email: novato@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>

guest_visitor:
  email: guest-fixture@guest.mappointr.local
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>
  guest: true
  display_name: "Visitante Teste"
