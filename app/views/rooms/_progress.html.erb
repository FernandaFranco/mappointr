<%# Quantos jogadores já responderam a rodada atual — variáveis: room_round %>

<p class="text-center text-sm text-gray-500">
  <%= room_round.game_rounds.count %> de <%= room_round.room.room_players.count %> responderam
</p>
